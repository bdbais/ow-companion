*.iml
.gradle/
/local.properties
/.idea/
.DS_Store
build/
/captures
.externalNativeBuild
.cxx
local.properties

# Data pipeline working files (raw wiki/API caches are re-fetchable)
/tools/__pycache__/
/tools/.cache/
/dataset/raw/

# Reference material dropped in by hand: top-down map images from statbanana.com, used as
# whiteboard backgrounds on one machine. They are someone else's work and tens of megabytes
# each, so they stay out of a public repository.
maps/

# Kotlin compiler crash logs, written into the working tree on a failed build.
.kotlin/

# The release signing key and its passwords. This repository is public, and this key is the
# only thing that lets an update install over a copy somebody already has: leaking it lets
# anyone publish an APK Android will accept as ours, and losing it ends updates for good.
*.jks
*.keystore
keystore.properties

# Reference sheets for the field panel's art. Kept out of the repository on purpose: the
# packed asset is what the app needs, and the sheets give away more than they are worth.
dataset/field/
